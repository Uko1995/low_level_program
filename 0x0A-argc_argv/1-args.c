#include <stdio.h>
#include "main.h"

/**
 * main - prints the number of arguments followed ba a new line
 * @argc: number of arguments
 * @argv: arguments vector
 *
 * Return: 0
 */

int main(int argc, char argv[] __attribute__((unused)))
{
	printf("%d\n", argc);
	return (0);
}
